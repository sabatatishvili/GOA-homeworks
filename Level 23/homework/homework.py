#def add_numbers(a, b):
    #try:
        #a = float(a) if isinstance(a, (str, bool, int, float)) else 0
        #b = float(b) if isinstance(b, (str, bool, int, float)) else 0
        #return a + b
    #except ValueError:
        #return "Invalid input: cannot convert to number"

#-------------------------------------------------------------------------

#def divide_numbers(a, b):
    #try:
        #a = float(a) if isinstance(a, (str, bool, int, float)) else 0
        #b = float(b) if isinstance(b, (str, bool, int, float)) else 0
        #if b == 0:
            #raise ZeroDivisionError("Cannot divide by zero")
        #return max(a, b) / min(a, b)
    #except ZeroDivisionError as e:
        #return str(e)
    #except ValueError:
        #return "Invalid input: cannot convert to number"

#-------------------------------------------------------------------

#def concat_strings(a, b):
    #try:
        #a = str(a) if isinstance(a, (int, str)) else ""
        #b = str(b) if isinstance(b, (int, str)) else ""
        #return a + b
    #except ValueError:
        #return "Invalid input: cannot convert to string"

#--------------------------------------------------------------------

#def is_string(a, b):
    #return isinstance(a, str)

#--------------------------------------------------------------------





